# 非技术用户部署步骤（Render）

## 你只需要做一次

### 1. 准备账号
需要：
- GitHub账号
- Render账号
- OpenAI API Key
- DeepSeek API Key（如果选择DeepSeek写教案）

### 2. 把程序放到GitHub
在GitHub新建一个私人仓库，例如：

ppt-lesson-plan-generator

把压缩包内文件上传到仓库根目录。

### 3. Render连接GitHub
在Render：
New → Web Service → Git Provider → 选择刚才仓库。

Language / Runtime 选择 Docker。

Render会读取 Dockerfile 自动安装：
- Python
- LibreOffice
- 中文字体
- 程序依赖

这些全部发生在云端，不会安装到你的工作电脑。

### 4. 填写环境变量
Render后台进入：
Environment

添加：

OPENAI_API_KEY
你的OpenAI API Key

DEEPSEEK_API_KEY
你的DeepSeek API Key

APP_PASSWORD
自己设置，例如一串只有你知道的密码

不要把API Key写到GitHub文件里。

### 5. 部署
点击 Deploy。

成功后会得到一个网址，例如：

https://你的应用名.onrender.com

以后公司电脑只打开这个网页即可。

---

# 日常使用

网页：
输入访问密码
→ 选择历史/地理/道法
→ 上传PPT
→ 上传原教学设计
→ 开始生成
→ 下载Word

不需要：
- Python
- CMD
- PowerShell
- 管理员权限
- 安装软件
- 每次输入API Key
